ในระบบ MAPS 11 มีการใช้ Google Apps Script (GAS) ทำหน้าที่เป็นระบบหลังบ้าน (Backend) และ API Server เพื่อบริหารจัดการข้อมูลก่อนส่งมาแสดงผลที่หน้าจอแอปพลิเคชัน โดยมีบทบาทสำคัญ 3 ส่วนหลักดังนี้

1. บริหารจัดการข้อมูลจุดความร้อนจากดาวเทียม (NASA Hotspots API)
โค้ดมีการเรียกใช้ gasWebappUrl พร้อมพารามิเตอร์ ?action=hotspots เพื่อดึงข้อมูลจุดความร้อน GAS จะทำหน้าที่เป็นตัวกลางไปดึงข้อมูลดิบจากดาวเทียม NASA (Suomi-NPP) นำมาจัดรูปแบบใหม่ให้เป็น JSON ที่อ่านง่ายขึ้น (มีข้อมูลละติจูด ลองจิจูด วันเวลา ค่าความเชื่อมั่น) ก่อนส่งให้ระบบหน้าบ้านนำไปจุดพิกัดบนแผนที่

2. สั่งบังคับอัปเดตข้อมูลดาวเทียมทันที (Force Sync)
เมื่อผู้ใช้งานกดปุ่ม "อัปเดตดาวเทียมเดี๋ยวนี้" ระบบหน้าบ้านจะส่งคำสั่ง ?action=force_sync_nasa ไปยัง GAS เพื่อสั่งให้เซิร์ฟเวอร์หลังบ้านไปกวาดข้อมูลจุดความร้อนชุดใหม่ล่าสุดจากระบบของ NASA ทันที โดยไม่ต้องรอรอบการอัปเดตตามรอบเวลาปกติ

3. เชื่อมต่อฐานข้อมูลสถานที่กำจัดขยะ (Landfill Database API)
ระบบมีการเรียกใช้ตัวแปร wasteGasUrl ซึ่งเป็น GAS อีกชุดหนึ่งที่ทำหน้าที่เชื่อมต่อกับฐานข้อมูลบ่อขยะที่ถูกจัดเก็บไว้ในรูปแบบ Google Sheets โดย GAS จะอ่านข้อมูลในตารางสเปรดชีต (เช่น ชื่อบ่อขยะ พิกัด ขนาดพื้นที่ ปริมาณขยะ สถานะการเปิดใช้งาน) แล้วแปลงสภาพให้เป็น JSON ส่งกลับมาให้หน้าบ้านวาดเป็นหมุดรูปถังขยะและใช้คำนวณความเสี่ยง

การประยุกต์ใช้ GAS ในลักษณะนี้ช่วยให้ระบบ MAPS 11 เป็น Serverless Application อย่างแท้จริง โดยไม่ต้องเช่าเซิร์ฟเวอร์ราคาแพง แถมเจ้าหน้าที่ยังสามารถเข้าไปแก้ไขเพิ่มลดข้อมูลบ่อขยะผ่านตาราง Google Sheets ได้อย่างสะดวกและข้อมูลบนแผนที่จะอัปเดตตามทันที
